# Local dev config for the mobile-purchase service. Copy to .env and adjust as needed.
# Matches infra/docker-compose.yml service "mobile-purchase-postgres" (port 5433).

NODE_ENV=development
PORT=8090
DATABASE_URL=postgresql://mobile_purchase:mobile_purchase_dev@localhost:5433/mobile_purchase
LOG_LEVEL=info
